/// App-group contract shared by the host app (writer) and keyboard (reader).
/// The 3.1 probe: whether the extension can read this WITHOUT Full Access.
let appGroupID = "group.com.jtsilverman.smartspace"
let probeKey = "appGroupProbe"
let probeValue = "probe-v1"
